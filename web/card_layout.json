{
  "schema": 1,
  "generated_by": "scripts/card_layout.py --json",
  "scan": {
    "max_name": 12,
    "min_bytes": 32768,
    "extensions": [
      "raw",
      "wav"
    ],
    "skip_dot": true
  },
  "encodings": {
    "u8": {
      "bits": 8,
      "wav_format": 1,
      "label": "unsigned 8-bit PCM (WAV AudioFormat 1)"
    },
    "int16": {
      "bits": 16,
      "wav_format": 1,
      "label": "signed 16-bit PCM (WAV AudioFormat 1)"
    },
    "int24": {
      "bits": 24,
      "wav_format": 1,
      "label": "signed 24-bit PCM (WAV AudioFormat 1)"
    },
    "int32": {
      "bits": 32,
      "wav_format": 1,
      "label": "signed 32-bit PCM (WAV AudioFormat 1)"
    },
    "f32": {
      "bits": 32,
      "wav_format": 3,
      "label": "32-bit IEEE float (WAV AudioFormat 3)"
    }
  },
  "rate_bounds": {
    "min": 4000,
    "max": 192000
  },
  "banks": [
    {
      "engine": "granular",
      "kind": "slots",
      "scanned": false,
      "dirs": [
        "SK/B",
        "SK/G",
        "SK/P",
        "SK/R",
        "SK/T",
        "SK/Y"
      ],
      "fmt": {
        "container": "wav",
        "encodings": [
          "f32",
          "int16"
        ],
        "channels": 2,
        "rate": 48000,
        "note": "The loop buffer is stereo, so a stereo file loads with no channel fold. Either depth is written straight into the buffer's own storage width.",
        "describe": "WAV, 32-bit IEEE float (WAV AudioFormat 3) or signed 16-bit PCM (WAV AudioFormat 1), STEREO, 48000 Hz"
      },
      "accepts": {
        "containers": [
          "wav"
        ],
        "encodings": [
          "u8",
          "int16",
          "int24",
          "int32",
          "f32"
        ],
        "max_channels": 8,
        "rate": null,
        "note": "Depth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.",
        "describe": "WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate"
      },
      "slots": [
        "1.WAV",
        "2.WAV",
        "3.WAV",
        "4.WAV",
        "5.WAV",
        "6.WAV"
      ],
      "max_files": null,
      "max_seconds": null,
      "sidecars": [],
      "source": "src/hw/card.cpp:61-66,131; src/memory/storage.cpp:14",
      "blurb": "Six colour-coded tapes (B G P R T Y), six slots each. Save with Alt+Play, load with Play. This is the platform's shared tape store, so the graincloud engine reads and writes the same folders.",
      "extras": {},
      "target": "SK/{tape}/{i}.WAV",
      "readers": [
        "granular",
        "graincloud"
      ]
    },
    {
      "engine": "tape",
      "kind": "slots",
      "scanned": false,
      "dirs": [
        "tapes"
      ],
      "fmt": {
        "container": "wav",
        "encodings": [
          "f32"
        ],
        "channels": 1,
        "rate": 48000,
        "note": "",
        "describe": "WAV, 32-bit IEEE float (WAV AudioFormat 3), MONO, 48000 Hz"
      },
      "accepts": {
        "containers": [
          "wav"
        ],
        "encodings": [
          "u8",
          "int16",
          "int24",
          "int32",
          "f32"
        ],
        "max_channels": 8,
        "rate": null,
        "note": "Depth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.",
        "describe": "WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate"
      },
      "slots": [
        "tape_a_1.wav",
        "tape_a_2.wav",
        "tape_a_3.wav",
        "tape_a_4.wav",
        "tape_a_5.wav",
        "tape_a_6.wav",
        "tape_a_7.wav",
        "tape_a_8.wav",
        "tape_b_1.wav",
        "tape_b_2.wav",
        "tape_b_3.wav",
        "tape_b_4.wav",
        "tape_b_5.wav",
        "tape_b_6.wav",
        "tape_b_7.wav",
        "tape_b_8.wav"
      ],
      "max_files": null,
      "max_seconds": null,
      "sidecars": [],
      "source": "src/engine/tape/tape_engine.cpp:397",
      "blurb": "8 slots per deck, selected with Alt+PITCH. Streams from the card, so files can be any length.",
      "extras": {},
      "target": "tapes/tape_{deck}_{i}.wav",
      "readers": [
        "tape"
      ]
    },
    {
      "engine": "shuttle",
      "kind": "slots",
      "scanned": false,
      "dirs": [
        "shuttle"
      ],
      "fmt": {
        "container": "wav",
        "encodings": [
          "f32"
        ],
        "channels": 1,
        "rate": 48000,
        "note": "",
        "describe": "WAV, 32-bit IEEE float (WAV AudioFormat 3), MONO, 48000 Hz"
      },
      "accepts": {
        "containers": [
          "wav"
        ],
        "encodings": [
          "u8",
          "int16",
          "int24",
          "int32",
          "f32"
        ],
        "max_channels": 8,
        "rate": null,
        "note": "Depth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.",
        "describe": "WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate"
      },
      "slots": [
        "tape_a_1.wav",
        "tape_a_2.wav",
        "tape_a_3.wav",
        "tape_a_4.wav",
        "tape_a_5.wav",
        "tape_a_6.wav",
        "tape_a_7.wav",
        "tape_a_8.wav",
        "tape_b_1.wav",
        "tape_b_2.wav",
        "tape_b_3.wav",
        "tape_b_4.wav",
        "tape_b_5.wav",
        "tape_b_6.wav",
        "tape_b_7.wav",
        "tape_b_8.wav"
      ],
      "max_files": null,
      "max_seconds": 30.0,
      "sidecars": [],
      "source": "src/engine/shuttle/shuttle_engine.cpp:520",
      "blurb": "Same format and slot names as tape, but LOADED into RAM: ~30 s per track, longer files truncate.",
      "extras": {},
      "target": "shuttle/tape_{deck}_{i}.wav",
      "readers": [
        "shuttle"
      ]
    },
    {
      "engine": "softcut",
      "kind": "slots",
      "scanned": false,
      "dirs": [
        "softcut"
      ],
      "fmt": {
        "container": "wav",
        "encodings": [
          "f32"
        ],
        "channels": 1,
        "rate": 48000,
        "note": "",
        "describe": "WAV, 32-bit IEEE float (WAV AudioFormat 3), MONO, 48000 Hz"
      },
      "accepts": {
        "containers": [
          "wav"
        ],
        "encodings": [
          "u8",
          "int16",
          "int24",
          "int32",
          "f32"
        ],
        "max_channels": 8,
        "rate": null,
        "note": "Depth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.",
        "describe": "WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate"
      },
      "slots": [
        "loop_a_1.wav",
        "loop_a_2.wav",
        "loop_a_3.wav",
        "loop_a_4.wav",
        "loop_a_5.wav",
        "loop_a_6.wav",
        "loop_a_7.wav",
        "loop_a_8.wav",
        "loop_b_1.wav",
        "loop_b_2.wav",
        "loop_b_3.wav",
        "loop_b_4.wav",
        "loop_b_5.wav",
        "loop_b_6.wav",
        "loop_b_7.wav",
        "loop_b_8.wav"
      ],
      "max_files": null,
      "max_seconds": 10.9,
      "sidecars": [],
      "source": "src/engine/softcut/softcut_engine.cpp:635-644; softcut_engine.h:133,149",
      "blurb": "Loops you record on the device: Alt+Seq/Alt+Rev save to the slot picked with Alt+PITCH, Play loads it. Same format as tape, in its own folder so the two cannot overwrite each other; ~10.9 s per loop, the size of the buffer.",
      "extras": {},
      "target": "softcut/loop_{deck}_{i}.wav",
      "readers": [
        "softcut"
      ]
    },
    {
      "engine": "radio",
      "kind": "scanned",
      "scanned": true,
      "dirs": [
        "radio/0",
        "radio/1",
        "radio/2",
        "radio/3",
        "radio/4",
        "radio/5",
        "radio/6",
        "radio/7",
        "radio/8",
        "radio/9",
        "radio/10",
        "radio/11",
        "radio/12",
        "radio/13",
        "radio/14",
        "radio/15"
      ],
      "fmt": {
        "container": "raw",
        "encodings": [
          "int16"
        ],
        "channels": 1,
        "rate": 48000,
        "note": "Headerless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).",
        "describe": "headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz"
      },
      "accepts": {
        "containers": [
          "raw",
          "wav"
        ],
        "encodings": [
          "u8",
          "int16",
          "int24",
          "int32",
          "f32"
        ],
        "max_channels": 8,
        "rate": null,
        "note": "A .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).",
        "describe": "headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate"
      },
      "slots": [],
      "max_files": 48,
      "max_seconds": null,
      "sidecars": [
        "radio/rate.txt"
      ],
      "source": "src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108",
      "blurb": "16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.",
      "extras": {
        "radio/rate.txt": "48000\n"
      },
      "target": "radio/{bank}/{i02}.raw",
      "readers": [
        "radio"
      ]
    },
    {
      "engine": "bard",
      "kind": "scanned",
      "scanned": true,
      "dirs": [
        "bard/0",
        "bard/1",
        "bard/2",
        "bard/3",
        "bard/4",
        "bard/5",
        "bard/6",
        "bard/7",
        "bard/8",
        "bard/9",
        "bard/10",
        "bard/11",
        "bard/12",
        "bard/13",
        "bard/14",
        "bard/15"
      ],
      "fmt": {
        "container": "wav",
        "encodings": [
          "int16"
        ],
        "channels": 1,
        "rate": null,
        "note": "int16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.",
        "describe": "WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate"
      },
      "accepts": {
        "containers": [
          "raw",
          "wav"
        ],
        "encodings": [
          "u8",
          "int16",
          "int24",
          "int32",
          "f32"
        ],
        "max_channels": 8,
        "rate": null,
        "note": "A .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).",
        "describe": "headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate"
      },
      "slots": [],
      "max_files": 32,
      "max_seconds": null,
      "sidecars": [
        "bard/BARD.CFG"
      ],
      "source": "src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132",
      "blurb": "16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.",
      "extras": {
        "bard/BARD.CFG": "resume=on\n"
      },
      "target": "bard/{bank}/BOOK{i02}.WAV",
      "readers": [
        "bard"
      ]
    },
    {
      "engine": "pstretch",
      "kind": "scanned",
      "scanned": true,
      "dirs": [
        "pstretch"
      ],
      "fmt": {
        "container": "wav",
        "encodings": [
          "int16"
        ],
        "channels": 1,
        "rate": null,
        "note": "int16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.",
        "describe": "WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate"
      },
      "accepts": {
        "containers": [
          "raw",
          "wav"
        ],
        "encodings": [
          "u8",
          "int16",
          "int24",
          "int32",
          "f32"
        ],
        "max_channels": 8,
        "rate": null,
        "note": "A .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).",
        "describe": "headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate"
      },
      "slots": [],
      "max_files": 32,
      "max_seconds": null,
      "sidecars": [],
      "source": "src/engine/pstretch/pstretch_engine.h:176-178",
      "blurb": "Source clips for the SD stretch source (Mode switch). 16-bit mono, any rate - off-rate clips are pitch-corrected. Long clips are ideal: at 50x a 3-minute file plays for ~2.5 h.",
      "extras": {},
      "target": "pstretch/CLIP{i02}.WAV",
      "readers": [
        "pstretch"
      ]
    },
    {
      "engine": "csound",
      "kind": "slots",
      "scanned": false,
      "dirs": [
        "csound"
      ],
      "fmt": {
        "container": "text",
        "encodings": [],
        "channels": null,
        "rate": null,
        "note": "",
        "describe": "plain text"
      },
      "accepts": {
        "containers": [
          "text"
        ],
        "encodings": [],
        "max_channels": 8,
        "rate": null,
        "note": "",
        "describe": "plain text"
      },
      "slots": [
        "0.csd",
        "1.csd",
        "2.csd",
        "3.csd",
        "4.csd",
        "5.csd",
        "6.csd",
        "7.csd"
      ],
      "max_files": null,
      "max_seconds": null,
      "sidecars": [],
      "source": "src/engine/csound/csound_patch.h:34",
      "blurb": "Up to 8 .csd orchestras, selected with Alt+PITCH. The patch defines the sound, not the firmware.",
      "extras": {},
      "target": "",
      "readers": [
        "csound"
      ]
    },
    {
      "engine": "chuck",
      "kind": "slots",
      "scanned": false,
      "dirs": [
        "chuck"
      ],
      "fmt": {
        "container": "text",
        "encodings": [],
        "channels": null,
        "rate": null,
        "note": "",
        "describe": "plain text"
      },
      "accepts": {
        "containers": [
          "text"
        ],
        "encodings": [],
        "max_channels": 8,
        "rate": null,
        "note": "",
        "describe": "plain text"
      },
      "slots": [
        "0.ck",
        "1.ck",
        "2.ck",
        "3.ck",
        "4.ck",
        "5.ck",
        "6.ck",
        "7.ck"
      ],
      "max_files": null,
      "max_seconds": null,
      "sidecars": [],
      "source": "src/engine/chuck/chuck_patch.h:49",
      "blurb": "Up to 8 .ck programs, selected with Alt+PITCH. Compiled at runtime by the ChucK VM.",
      "extras": {},
      "target": "",
      "readers": [
        "chuck"
      ]
    },
    {
      "engine": "platform",
      "kind": "config",
      "scanned": false,
      "dirs": [
        "SK"
      ],
      "fmt": {
        "container": "text",
        "encodings": [],
        "channels": null,
        "rate": null,
        "note": "",
        "describe": "plain text"
      },
      "accepts": {
        "containers": [
          "text"
        ],
        "encodings": [],
        "max_channels": 8,
        "rate": null,
        "note": "",
        "describe": "plain text"
      },
      "slots": [],
      "max_files": null,
      "max_seconds": null,
      "sidecars": [
        "SK/config.txt"
      ],
      "source": "src/memory/storage.h:22-23",
      "blurb": "Platform config and saved state. config.txt sets MIDI channels and boot preload; MEM is written by the device.",
      "extras": {},
      "target": "",
      "readers": [
        "platform"
      ]
    }
  ],
  "all_dirs": [
    "SK",
    "SK/B",
    "SK/G",
    "SK/P",
    "SK/R",
    "SK/T",
    "SK/Y",
    "tapes",
    "shuttle",
    "softcut",
    "radio",
    "radio/0",
    "radio/1",
    "radio/2",
    "radio/3",
    "radio/4",
    "radio/5",
    "radio/6",
    "radio/7",
    "radio/8",
    "radio/9",
    "radio/10",
    "radio/11",
    "radio/12",
    "radio/13",
    "radio/14",
    "radio/15",
    "bard",
    "bard/0",
    "bard/1",
    "bard/2",
    "bard/3",
    "bard/4",
    "bard/5",
    "bard/6",
    "bard/7",
    "bard/8",
    "bard/9",
    "bard/10",
    "bard/11",
    "bard/12",
    "bard/13",
    "bard/14",
    "bard/15",
    "pstretch",
    "csound",
    "chuck"
  ],
  "granular_tapes": [
    "B",
    "G",
    "P",
    "R",
    "T",
    "Y"
  ],
  "default_config": "mid_ch_a\n1\nmid_ch_b\n2\nmid_ps_a\n0\nmid_ps_b\n0\npre_load\n1\n",
  "config_properties": {
    "mid_ch_a": [
      1,
      16
    ],
    "mid_ch_b": [
      1,
      16
    ],
    "mid_ps_a": [
      0,
      1
    ],
    "mid_ps_b": [
      0,
      1
    ],
    "pre_load": [
      0,
      1
    ]
  },
  "source_extensions": [
    ".mp3",
    ".flac",
    ".m4a",
    ".aac",
    ".ogg",
    ".opus",
    ".aiff",
    ".aif",
    ".wma",
    ".alac"
  ],
  "sidecar_names": [
    "README.TXT",
    "BOOKS.TXT",
    "RATE.TXT",
    "BARD.CFG",
    "CONFIG.TXT",
    "MEM"
  ],
  "skip_dirs": [
    "System Volume Information",
    ".Spotlight-V100",
    ".Trashes",
    ".fseventsd"
  ],
  "readmes": {
    "SK/B": "GRANULAR - what goes in this folder\r\n==============================================\r\n\r\nSix colour-coded tapes (B G P R T Y), six slots each. Save with Alt+Play, load with Play. This is the platform's shared tape store, so the graincloud engine reads and writes the same folders.\r\n\r\nACCEPTS: WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nDepth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.\r\n\r\nBEST: WAV, 32-bit IEEE float (WAV AudioFormat 3) or signed 16-bit PCM (WAV AudioFormat 1), STEREO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nThe loop buffer is stereo, so a stereo file loads with no channel fold. Either depth is written straight into the buffer's own storage width.\r\n\r\nFILENAMES: exactly 1.WAV, 2.WAV, 3.WAV, 4.WAV, ... (6 total)\r\nAny other name is never opened - the slot just reads as empty.\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/hw/card.cpp:61-66,131; src/memory/storage.cpp:14\r\n",
    "SK/G": "GRANULAR - what goes in this folder\r\n==============================================\r\n\r\nSix colour-coded tapes (B G P R T Y), six slots each. Save with Alt+Play, load with Play. This is the platform's shared tape store, so the graincloud engine reads and writes the same folders.\r\n\r\nACCEPTS: WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nDepth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.\r\n\r\nBEST: WAV, 32-bit IEEE float (WAV AudioFormat 3) or signed 16-bit PCM (WAV AudioFormat 1), STEREO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nThe loop buffer is stereo, so a stereo file loads with no channel fold. Either depth is written straight into the buffer's own storage width.\r\n\r\nFILENAMES: exactly 1.WAV, 2.WAV, 3.WAV, 4.WAV, ... (6 total)\r\nAny other name is never opened - the slot just reads as empty.\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/hw/card.cpp:61-66,131; src/memory/storage.cpp:14\r\n",
    "SK/P": "GRANULAR - what goes in this folder\r\n==============================================\r\n\r\nSix colour-coded tapes (B G P R T Y), six slots each. Save with Alt+Play, load with Play. This is the platform's shared tape store, so the graincloud engine reads and writes the same folders.\r\n\r\nACCEPTS: WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nDepth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.\r\n\r\nBEST: WAV, 32-bit IEEE float (WAV AudioFormat 3) or signed 16-bit PCM (WAV AudioFormat 1), STEREO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nThe loop buffer is stereo, so a stereo file loads with no channel fold. Either depth is written straight into the buffer's own storage width.\r\n\r\nFILENAMES: exactly 1.WAV, 2.WAV, 3.WAV, 4.WAV, ... (6 total)\r\nAny other name is never opened - the slot just reads as empty.\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/hw/card.cpp:61-66,131; src/memory/storage.cpp:14\r\n",
    "SK/R": "GRANULAR - what goes in this folder\r\n==============================================\r\n\r\nSix colour-coded tapes (B G P R T Y), six slots each. Save with Alt+Play, load with Play. This is the platform's shared tape store, so the graincloud engine reads and writes the same folders.\r\n\r\nACCEPTS: WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nDepth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.\r\n\r\nBEST: WAV, 32-bit IEEE float (WAV AudioFormat 3) or signed 16-bit PCM (WAV AudioFormat 1), STEREO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nThe loop buffer is stereo, so a stereo file loads with no channel fold. Either depth is written straight into the buffer's own storage width.\r\n\r\nFILENAMES: exactly 1.WAV, 2.WAV, 3.WAV, 4.WAV, ... (6 total)\r\nAny other name is never opened - the slot just reads as empty.\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/hw/card.cpp:61-66,131; src/memory/storage.cpp:14\r\n",
    "SK/T": "GRANULAR - what goes in this folder\r\n==============================================\r\n\r\nSix colour-coded tapes (B G P R T Y), six slots each. Save with Alt+Play, load with Play. This is the platform's shared tape store, so the graincloud engine reads and writes the same folders.\r\n\r\nACCEPTS: WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nDepth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.\r\n\r\nBEST: WAV, 32-bit IEEE float (WAV AudioFormat 3) or signed 16-bit PCM (WAV AudioFormat 1), STEREO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nThe loop buffer is stereo, so a stereo file loads with no channel fold. Either depth is written straight into the buffer's own storage width.\r\n\r\nFILENAMES: exactly 1.WAV, 2.WAV, 3.WAV, 4.WAV, ... (6 total)\r\nAny other name is never opened - the slot just reads as empty.\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/hw/card.cpp:61-66,131; src/memory/storage.cpp:14\r\n",
    "SK/Y": "GRANULAR - what goes in this folder\r\n==============================================\r\n\r\nSix colour-coded tapes (B G P R T Y), six slots each. Save with Alt+Play, load with Play. This is the platform's shared tape store, so the graincloud engine reads and writes the same folders.\r\n\r\nACCEPTS: WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nDepth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.\r\n\r\nBEST: WAV, 32-bit IEEE float (WAV AudioFormat 3) or signed 16-bit PCM (WAV AudioFormat 1), STEREO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nThe loop buffer is stereo, so a stereo file loads with no channel fold. Either depth is written straight into the buffer's own storage width.\r\n\r\nFILENAMES: exactly 1.WAV, 2.WAV, 3.WAV, 4.WAV, ... (6 total)\r\nAny other name is never opened - the slot just reads as empty.\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/hw/card.cpp:61-66,131; src/memory/storage.cpp:14\r\n",
    "tapes": "TAPE - what goes in this folder\r\n==============================================\r\n\r\n8 slots per deck, selected with Alt+PITCH. Streams from the card, so files can be any length.\r\n\r\nACCEPTS: WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nDepth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.\r\n\r\nBEST: WAV, 32-bit IEEE float (WAV AudioFormat 3), MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nFILENAMES: exactly tape_a_1.wav, tape_a_2.wav, tape_a_3.wav, tape_a_4.wav, ... (16 total)\r\nAny other name is never opened - the slot just reads as empty.\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/tape/tape_engine.cpp:397\r\n",
    "shuttle": "SHUTTLE - what goes in this folder\r\n==============================================\r\n\r\nSame format and slot names as tape, but LOADED into RAM: ~30 s per track, longer files truncate.\r\n\r\nACCEPTS: WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nDepth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.\r\n\r\nBEST: WAV, 32-bit IEEE float (WAV AudioFormat 3), MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nFILENAMES: exactly tape_a_1.wav, tape_a_2.wav, tape_a_3.wav, tape_a_4.wav, ... (16 total)\r\nAny other name is never opened - the slot just reads as empty.\r\n\r\nLENGTH: about 30 seconds max - longer files are truncated on load.\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/shuttle/shuttle_engine.cpp:520\r\n",
    "softcut": "SOFTCUT - what goes in this folder\r\n==============================================\r\n\r\nLoops you record on the device: Alt+Seq/Alt+Rev save to the slot picked with Alt+PITCH, Play loads it. Same format as tape, in its own folder so the two cannot overwrite each other; ~10.9 s per loop, the size of the buffer.\r\n\r\nACCEPTS: WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nDepth, channel count and sample rate are all converted as the file loads, so any of these combinations plays correctly. Converting on a computer first is still worth it - see BEST below - but it is an optimisation now, not a requirement.\r\n\r\nBEST: WAV, 32-bit IEEE float (WAV AudioFormat 3), MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nFILENAMES: exactly loop_a_1.wav, loop_a_2.wav, loop_a_3.wav, loop_a_4.wav, ... (16 total)\r\nAny other name is never opened - the slot just reads as empty.\r\n\r\nLENGTH: about 11 seconds max - longer files are truncated on load.\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/softcut/softcut_engine.cpp:635-644; softcut_engine.h:133,149\r\n",
    "radio/0": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/1": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/2": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/3": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/4": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/5": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/6": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/7": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/8": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/9": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/10": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/11": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/12": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/13": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/14": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "radio/15": "RADIO - what goes in this folder\r\n==============================================\r\n\r\n16 banks of up to 48 stations. Headerless .raw at 48 kHz; a .wav is also accepted and carries its own rate.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: headerless RAW, signed 16-bit PCM, little-endian, MONO, 48000 Hz\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nHeaderless, for RadioMusic-compatible cards: nothing in the file states its format, so the rate is fixed by convention (or radio/rate.txt).\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 48 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/radio/radio_engine.cpp:306; radio_engine.h:107-108\r\n",
    "bard/0": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/1": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/2": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/3": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/4": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/5": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/6": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/7": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/8": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/9": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/10": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/11": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/12": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/13": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/14": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "bard/15": "BARD - what goes in this folder\r\n==============================================\r\n\r\n16 shelves of up to 32 books. 16-bit MONO; 24 kHz is the right rate for speech (half the bytes per hour). Each BOOK.WAV may have a BOOK.TXT of bookmarks beside it, and BOOKS.TXT maps 8.3 names back to real titles.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/bard/bard_engine.cpp:872; bard_engine.h:131-132\r\n",
    "pstretch": "PSTRETCH - what goes in this folder\r\n==============================================\r\n\r\nSource clips for the SD stretch source (Mode switch). 16-bit mono, any rate - off-rate clips are pitch-corrected. Long clips are ideal: at 50x a 3-minute file plays for ~2.5 h.\r\n\r\nACCEPTS: headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n\r\nA .wav is converted as it loads, so any depth, channel count or rate works. A .raw is HEADERLESS - nothing in the file states its format - so it must be exactly 16-bit mono, at the rate fixed by convention (or radio/rate.txt).\r\n\r\nBEST: WAV, signed 16-bit PCM (WAV AudioFormat 1), MONO, any sample rate\r\nThat is what this engine works in natively, so it loads with no conversion.\r\n`sk_card.py convert` writes exactly this.\r\n\r\nint16 mono is what these engines stream natively; the header carries the rate, so an off-rate file plays at correct pitch.\r\n\r\nTHIS FOLDER IS SCANNED, so these rules also apply - break one and the file is\r\nINVISIBLE to the device, with no error shown:\r\n  - name must be 12 characters or fewer, including the extension\r\n  - extension must be .raw or .wav\r\n  - file must be at least 32 KB\r\n  - names starting with a dot are skipped (macOS ._* and .DS_Store)\r\n  - at most 32 files are indexed per folder\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/pstretch/pstretch_engine.h:176-178\r\n",
    "csound": "CSOUND - what goes in this folder\r\n==============================================\r\n\r\nUp to 8 .csd orchestras, selected with Alt+PITCH. The patch defines the sound, not the firmware.\r\n\r\nACCEPTS: plain text\r\n\r\nFILENAMES: exactly 0.csd, 1.csd, 2.csd, 3.csd, ... (8 total)\r\nAny other name is never opened - the slot just reads as empty.\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/csound/csound_patch.h:34\r\n",
    "chuck": "CHUCK - what goes in this folder\r\n==============================================\r\n\r\nUp to 8 .ck programs, selected with Alt+PITCH. Compiled at runtime by the ChucK VM.\r\n\r\nACCEPTS: plain text\r\n\r\nFILENAMES: exactly 0.ck, 1.ck, 2.ck, 3.ck, ... (8 total)\r\nAny other name is never opened - the slot just reads as empty.\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/engine/chuck/chuck_patch.h:49\r\n",
    "SK": "PLATFORM - what goes in this folder\r\n==============================================\r\n\r\nPlatform config and saved state. config.txt sets MIDI channels and boot preload; MEM is written by the device.\r\n\r\nACCEPTS: plain text\r\n\r\nCheck a card you built with:   python3 scripts/sk_card.py verify /path/to/card\r\nFirmware reference: src/memory/storage.h:22-23\r\n"
  },
  "root_readme": {
    "demo": "SPOTYKACH SD CARD\r\n==============================================\r\n\r\nFormat the card as FAT32 (up to 32 GB). Each folder here belongs to one engine;\r\nopen its README.TXT for the exact audio format that folder needs.\r\n\r\nThe firmware converts sample depth and channel count as it loads, but never the SAMPLE\r\nRATE - and a headerless .raw states no format at all, so a wrong one plays as noise.\r\nCheck a card with:\r\n\r\n    python3 scripts/sk_card.py verify /path/to/card\r\n\r\nAdd your own audio with:\r\n\r\n    python3 scripts/sk_card.py convert --engine tape /path/to/card mysound.mp3\r\n\r\nFolders:\r\n  SK/B         granular   WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  tapes        tape       WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  shuttle      shuttle    WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  softcut      softcut    WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  radio/0      radio      headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  bard/0       bard       headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  pstretch     pstretch   headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  csound       csound     plain text\r\n  chuck        chuck      plain text\r\n  SK           platform   plain text\r\n\r\nThe audio on this card is synthesized placeholder content so that every engine\r\nmakes a sound out of the box. Replace it with your own.\r\n",
    "bare": "SPOTYKACH SD CARD\r\n==============================================\r\n\r\nFormat the card as FAT32 (up to 32 GB). Each folder here belongs to one engine;\r\nopen its README.TXT for the exact audio format that folder needs.\r\n\r\nThe firmware converts sample depth and channel count as it loads, but never the SAMPLE\r\nRATE - and a headerless .raw states no format at all, so a wrong one plays as noise.\r\nCheck a card with:\r\n\r\n    python3 scripts/sk_card.py verify /path/to/card\r\n\r\nAdd your own audio with:\r\n\r\n    python3 scripts/sk_card.py convert --engine tape /path/to/card mysound.mp3\r\n\r\nFolders:\r\n  SK/B         granular   WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  tapes        tape       WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  shuttle      shuttle    WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  softcut      softcut    WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  radio/0      radio      headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  bard/0       bard       headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  pstretch     pstretch   headerless RAW or WAV, 8-bit / 16-bit / 24-bit / 32-bit int / 32-bit float, mono or stereo, any sample rate\r\n  csound       csound     plain text\r\n  chuck        chuck      plain text\r\n  SK           platform   plain text\r\n"
  }
}
